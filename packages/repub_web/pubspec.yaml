name: repub_web
description: Web UI for Repub package registry built with Jaspr
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  repub_model:
  jaspr: ^0.22.0
  jaspr_router: ^0.8.0
  http: ^1.2.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  jaspr_builder: ^0.22.0
  lints: ^5.0.0
