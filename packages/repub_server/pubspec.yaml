name: repub_server
description: HTTP server for repub package registry
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  repub_model:
    path: ../repub_model
  repub_auth:
    path: ../repub_auth
  repub_storage:
    path: ../repub_storage
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  shelf_proxy: ^1.0.4
  archive: ^4.0.2
  yaml: ^3.1.3
  crypto: ^3.0.6
  path: ^1.9.0
  http: ^1.2.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
